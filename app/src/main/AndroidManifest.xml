<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.guanyue.everydaynews">

    <application
        android:name=".base.ApplicationPlus"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- 设置申请的 AppId 和 AppKey -->
        <meta-data
            android:name="TTC_APPID"
            android:value="HBGY32SGPT"/>
        <meta-data
            android:name="TTC_APPKEY"
            android:value="7f27388d37064ad5a114b41d2fe84a1d"/>

        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity">
        </activity>
    </application>

</manifest>